### deploy to heroku
# install heroku cli from web
# ensure heroku in env path -> heroku --version
# login -> heroku login
# make procfile
# make setup.sh file
# potentially create .gitignore file for venv and others
# init git -> git init
# add all code to git -> git add 
# push to github repo
# create app in heroku -> heroku create <appname>
# align git repo with heroku -> heroku git:remote -a photo-enhancer-webapp
# git add .
# git commit -am "to heroku"
# git push heroku master